---
// NavItem.astro
let { href, currentPath, children } = Astro.props;
let isActive = currentPath === href;
---

<a class={isActive ? 'active' : ''} href={href}>
  <slot />
</a>

<style>
  .active {
    border: 1px solid blue;
  }
</style>